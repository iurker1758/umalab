# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
# node
node_modules/
dist/
dev-dist/
*.tsbuildinfo
# local dev launchers — machine-specific, not part of the project
dev.ps1
dev.cmd

# env & editors
.env
.DS_Store
.idea/
.vscode/
# secrets — the uma.moe API key must never land in the repo
uma_api_key.txt
# game art (Cygames') — fetched locally by scripts, never committed
frontend/public/icons/
# wrangler pages dev state
.wrangler/
